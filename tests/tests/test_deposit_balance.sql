--deposit ballance update testi--
--mevcut bakiye kontrolü--
SELECT account_id, balance FROM accounts WHERE account_id = 2
;

--sonraki bakiye kontrolu--
SELECT account_id, balance FROM accounts WHERE account_id = 2;

